# Overwrite parameters here
parameters:
  kyverno: {}

  kapitan:
    ~compile: []

    compile:
      - input_type: jsonnet
        input_paths:
          - tests/test-patch.jsonnet
        output_path: tests/
      - input_type: jsonnet
        input_paths:
          - tests/test-patch-long-name.jsonnet
        output_path: tests/
      - input_type: jsonnet
        input_paths:
          - tests/test-patch-rbac.jsonnet
        output_path: tests/
