parameters:
  kyverno:
    namespace: syn-kyverno
    images:
      kyverno:
        registry: ghcr.io
        repository: kyverno/kyverno
        version: v1.4.2
      pre:
        registry: ghcr.io
        repository: kyverno/kyvernopre
        version: ${kyverno:images:kyverno:version}
    kyverno:
      manifest_version: ${kyverno:images:kyverno:version}
      manifest_url: https://raw.githubusercontent.com/kyverno/kyverno/${kyverno:kyverno:manifest_version}/definitions
