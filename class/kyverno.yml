parameters:
  kapitan:
    compile:
      - input_paths:
          - kyverno/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - kyverno/component/main.jsonnet
        input_type: jsonnet
        output_path: kyverno/
